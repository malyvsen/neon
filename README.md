# neon
macOS setup scripts and dotfiles.

## Setup
Run `curl https://raw.githubusercontent.com/malyvsen/neon/main/setup.sh | bash`. You will be asked to enter the sudo password a few times, so check periodically.

The same command updates an existing machine: it installs missing packages (including Docker Desktop), refreshes dotfiles, and leaves extra programs alone.
