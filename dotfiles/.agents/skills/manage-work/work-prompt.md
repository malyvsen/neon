Create a worktree for yourself and check out a new branch named {{subagent's branch, made up of the managed branch name plus a suffix matching the subagent's name}} in it. Start from the {{managed branch}} branch.

{{if a plan is provided}}
Once the worktree is set up, execute the plan at {{path to plan}}.
{{else provide instructions as needed}}
{{/if}}
